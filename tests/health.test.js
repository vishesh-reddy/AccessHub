const test=require('node:test');const assert=require('node:assert/strict');const app=require('../backend/server');const http=require('node:http');test('health endpoint returns ok',async()=>{const server=http.createServer(app);await new Promise(r=>server.listen(0,r));const res=await fetch('http://127.0.0.1:'+server.address().port+'/api/health');const body=await res.json();assert.equal(res.status,200);assert.equal(body.status,'ok');await new Promise(r=>server.close(r));});